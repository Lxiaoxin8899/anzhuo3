<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- 声明需要探测的TTS相关包，解决Android 11+的包可见性限制 -->
    <queries>
        <package android:name="com.xiaomi.mibrain.speech" />
        <package android:name="com.miui.tts" />
        <package android:name="com.xiaomi.speech" />
        <package android:name="com.miui.speech.tts" />
        <package android:name="com.google.android.tts" />
        <intent>
            <action android:name="android.speech.tts.engine.CHECK_TTS_DATA" />
        </intent>
        <intent>
            <action android:name="android.speech.tts.engine.GET_SAMPLE_TEXT" />
        </intent>
    </queries>

    <!-- 网络权限 -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />

    <!-- 语音播放权限 -->
    <uses-permission android:name="android.permission.RECORD_AUDIO" />
    <uses-permission android:name="android.permission.MODIFY_AUDIO_SETTINGS" />
    <uses-permission android:name="android.permission.BIND_ACCESSIBILITY_SERVICE" />

    <!-- 小米设备特殊权限 -->
    <uses-permission android:name="android.permission.SYSTEM_ALERT_WINDOW" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />

    <!-- 小米TTS专用权限 -->
    <uses-permission android:name="com.xiaomi.permission.TTS" />
    <uses-permission android:name="com.miui.permission.TTS" />
    <uses-permission android:name="android.permission.BIND_TTS_SERVICE" />

    <!-- 音频焦点权限 -->
    <uses-permission android:name="android.permission.AUDIO_FOCUS" />
    <uses-permission android:name="android.permission.ACCESS_AUDIO_STATE" />

    <application
        android:allowBackup="true"
        android:hasFragileUserData="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.SmartDosing"
        android:usesCleartextTraffic="true">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:theme="@style/Theme.SmartDosing">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>
